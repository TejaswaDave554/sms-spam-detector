# Models Directory

This directory stores trained model files.

## Files
- `spam-sms-mnb-model.pkl` - Trained Multinomial Naive Bayes model
- `cv-transform.pkl` - CountVectorizer for text transformation

## Note
Model files are not tracked in git. Run `python train_model.py` to generate them.
